import Node from '../src/node';
export default class Expression extends Node {
  /*
   * @constructor
   */
  constructor(){
    super();
    this.node = undefined;
  }
  /*
   * get expressionNode
   */
  expressionNode(){
  }
}
