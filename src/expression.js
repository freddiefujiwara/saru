import Node from '../src/node';
export default class Expression extends Node {
  /*
   * @constructor
   */
  constructor(token = undefined){
    super(token);
  }
  /*
   * get expressionNode
   */
  expressionNode(){
  }
}
