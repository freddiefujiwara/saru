import Node from '../src/node';
export default class Statement extends Node {
  /*
   * @constructor
   */
  constructor(){
    super();
    this.node = undefined;
  }
  /*
   * get statementNode
   */
  statementNode(){
  }
}
