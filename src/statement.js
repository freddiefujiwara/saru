import Node from '../src/node';
export default class Statement extends Node {
  /*
   * get statementNode
   */
  statementNode(){
  }
  /*
   * @constructor
   */
  constructor(token = undefined){
    super(token);
  }
}
